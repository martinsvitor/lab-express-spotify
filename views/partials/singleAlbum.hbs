<div class="card">
    <div class="images">
        <img src="{{this.images.0.url}}" alt="{{this.name}} Cd cover"/>
    </div>
    <h5>{{this.name}}</h5>
    <a href="/albums/tracks/{{this.id}}">View Tracks</a>
</div>
